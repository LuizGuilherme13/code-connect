{
  "name": "code-connect",
  "private": true,
  "scripts": {
    "dev": "bash dev.sh",
    "dev:frontend": "cd frontend && npx vite",
    "dev:backend": "cd backend && go run .",
    "build": "npm run build --prefix frontend",
    "build:backend": "cd backend && go build -o ../server .",
    "start:backend": "cd backend && go run ."
  }
}
